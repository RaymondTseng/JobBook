<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jobbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".main.widget.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>

        </activity>
        <activity android:name=".job.widget.JobDetailActivity"/>
        <activity android:name=".article.widget.ArticleDetailActivity"/>
        <activity android:name=".person.widget.SettingActivity"/>
        <activity android:name=".person.widget.FavouriteActivity"/>
        <activity android:name=".question.widget.QuestionDetailActivity"/>

        <activity android:name=".chat.widget.ChatActivity"/>

        <activity android:name=".feedback.widget.FeedBackActivity" />

        <activity android:name=".job.widget.CompanyDetailActivity" />

        <activity android:name=".question.widget.NewQuestionActivity" />

    </application>



</manifest>